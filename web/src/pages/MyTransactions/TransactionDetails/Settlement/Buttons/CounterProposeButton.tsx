import React from "react";
import { Button } from "@kleros/ui-components-library";

const CounterProposeButton: React.FC = () => {
  return <Button text={"Counter-propose"} />;
};
export default CounterProposeButton;
