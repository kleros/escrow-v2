import React from "react";
import { Button } from "@kleros/ui-components-library";

const DepositFeeButton: React.FC = () => {
  return <Button text={"Deposit the fee: 0.001 ETH"} />;
};
export default DepositFeeButton;
